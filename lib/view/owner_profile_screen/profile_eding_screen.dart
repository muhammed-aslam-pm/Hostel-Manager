import 'package:flutter/material.dart';

class ProfileEdingScreen extends StatelessWidget {
  const ProfileEdingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
